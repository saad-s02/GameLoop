# BUILDLOG.md: build incidents, captured in real time

Format per incident: what happened / how it was caught / correction / lesson.

## 2026-07-14 build start

Phase 2 build began from the approved design spec. Incidents append below as they happen.

## 2026-07-14 Fixture B goalie save count contradicts the research projection

What happened: the implementation plan pinned Fixture B's top three moments assuming no goalie reached the 35-save goalie-performance threshold, extrapolated from research/02's shots-on-goal figure. The Wave 0 boxscore fetch (13,522 bytes, gamecenter/2025030313/boxscore) shows Montreal's Jakub Dobes with 36 saves on 39 shots.

How it was caught: the plan's Task 4 verification step explicitly checks the 35-save threshold against the fetched boxscore before any moments code exists.

Correction: kept the threshold at 35 rather than tuning it to dodge real data, and re-pinned Fixture B's expected top three to [Svechnikov OT winner, Dobes goalie performance, Hutson power-play goal]. MomentSchema.memberPlays no longer requires a minimum of one, since goalie moments are boxscore-derived and have no play events.

Lesson: derived projections (SOG minus goals, split across goalies) are not verified facts. Pin expectations only against data actually fetched, and build verification steps that fire before the dependent code is written.

## 2026-07-14 Wave 0 environment gaps (deploy auth, API key)

What happened: the Vercel CLI token is invalid and the Vercel MCP integration lacks project-create permission, so the skeleton deploy is deferred pending a one-time vercel login. The ANTHROPIC_API_KEY in .env.local is well-formed (sk-ant-api..., 108 chars) but the API returns 401 invalid x-api-key, so the lib/ai spike's live confirmation is deferred pending a valid key.

How it was caught: Task 2 deploy attempt returned 403 forbidden on project creation; Task 3 spike run returned 401 from api.anthropic.com.

Correction: proceeding with Tasks 4 and 5 (fixture fetch, schema lock) and Wave 1, which have no cloud dependencies. Partial spike finding banked from the 401 error dump: the AI SDK v7 providerOptions path anthropic.thinking {type: disabled} maps to a top-level thinking field in the outgoing request body, and maxOutputTokens maps to max_tokens.

Lesson: prove credentials before the wave that needs them; the error dump of a failed call still carries request-shape evidence worth recording.

## 2026-07-14 Venue authoring arithmetic missed a wait-band crossing

What happened: the plan authored gate-1's wait profile with 10 minutes in the 18:30 to 19:00 band, but the spec's +18 disruption story (18:33 arrival seated about 18:48) assumed the same 6-minute wait as the baseline path. The delayed family reaches gate-1 at 18:41, inside the later band, so the authored numbers produced 18:52.

How it was caught: the venue consistency test pinned both outcomes exactly; the Task 7 implementation agent ran it, got -38 instead of -42, verified its transcription byte-for-byte against the brief, and reported BLOCKED instead of tuning the data.

Correction: main thread set gate-1's 18:30 to 19:00 band to 6 minutes, which lands the disrupted path at exactly 18:48 while touching no other pinned path (the 18:15 baseline uses the earlier band, and the gate-wait disruption overrides every band to 22). Plan document updated to match.

Lesson: authored fixture arithmetic must be traced through every band boundary it crosses, and the block-instead-of-tune rule did its job: the agent surfaced the contradiction instead of silently bending the data.

## 2026-07-14 The gate-wait disruption pin named the wrong gate

What happened: the plan pinned the "Gate 1 wait rises to 22" disruption as flipping the family plan to gate-5b, mirroring the PRD's illustrative Gate 5B prose. The planner implementation agent began verifying gate-5b's stand connectivity and the main thread completed the trace: gate-5b's two reachable stands offer no gluten-free item, so no gate-5b candidate can cover the hard dietary constraint, and gate-5b's own timing misses warmups anyway. Under the full four-gate arithmetic, gate-3 dominates (gluten-free via North Shore Grill, seated 18:34, gate wait 7 against the disrupted 22).

How it was caught: the implementer treated a pinned disruption expectation as something to verify against the authored venue before coding to it, and the main thread finished the arithmetic across all four gates.

Correction: conscious re-pin of the disruption expectation to gate-3. The venue data was not touched; it had already passed review, and bending data to rescue a pin is the failure mode the rules exist to prevent. The PRD's Gate 5B passage is illustrative prose about the kind of trade-off the explanation should narrate, not a binding requirement; the Decision Log narrates the real computed numbers.

Lesson: a pin derived from a two-way comparison is not a pin about the world; expectations must be derived against the full enumeration space they will be tested in.

## 2026-07-14 A stale environment variable shadowed every key fix

What happened: the API key kept returning 401 through three separate key rotations, and every diagnostic pointed at the .env.local file. The real cause: an old ANTHROPIC_API_KEY set as a Windows user environment variable. Node's --env-file and Next's env loading never override an already-set process variable, so every run silently used the dead key while the file held a perfectly good one. The user's edits had been landing correctly for some time.

How it was caught: the user pushed back that the key was definitely correct, which forced a re-examination of the assumption that process.env reflected the file; reading the file's raw bytes and the shell environment separately showed two different keys.

Correction: local scripts run with env -u ANTHROPIC_API_KEY until the Windows variable is removed; ADR-002 completed immediately afterward. During diagnosis the dead key's value was echoed into the session log; it is invalid, and the recommendation to revoke it in the console was passed to the user.

Lesson: when a fix that obviously should work repeatedly does not, stop debugging the fix and re-verify the assumption underneath it, and listen when the human says the fix is correct. Precedence between environment variables and env files is exactly the kind of invisible layer that produces this failure.

## 2026-07-14 Wave 3A integration measurements (local production server)

Not an incident, a record. First full live journey against the built app, one venue machine, intro-tier API:

Demo chip (zero model calls): full event sequence correct, contract card event strictly first, snap and flood control present. Live family prompt, cold: 14.8 s including the one-time extraction-schema grammar compile. Live family prompt, warm: 7.7 s against the 12 s budget. Extraction reproduced all five pinned constraints and the 6:18 to 18:15 snap verbatim. Replan with train-plus-18: 8.7 s against the 8 s budget, seated 18:48 exactly, warmups traded, dietary preserved, diff 6 kept 1 dropped 1 replaced. Tuning item for the eval pass: replans re-stream a full explanation; a shorter replan-specific explanation or smaller max tokens should close the 0.7 s. Live recap with session bridge, cold: 13.0 s against the 15 s budget including the recap schema grammar compile; scoreLine echoed exactly, fell-short framing honest, centre-ice bridge factual, no banned market strings.

One spec gap found by review and closed during integration: demo mode without a chip could fall through to a live extraction call; it now refuses with a scoped decision event, keeping the zero-LLM guarantee unconditional (covered by a route test).

## 2026-07-14 Production verification record

Deployed URL (present from this one): https://gameloop-gilt.vercel.app. Deployment Protection stays on Standard (previews and raw deployment URLs behind SSO, production alias public behind the app's own access code plus noindex); the earlier SSO 302s were probes against the team-scoped deployment URL, which is protected by design. Deployed Playwright smoke (full scripted demo sequence with the real access code): passed in 16.1 s. Production warmup after the final deploy: extraction grammar 1,667 ms, recap grammar 5,973 ms; compiled grammars cache roughly 24 hours, so this repeats from the venue network 15 minutes before the Thursday demo. Environment variables live in Vercel Production: ANTHROPIC_API_KEY and ACCESS_COOKIE_SECRET Sensitive, ACCESS_CODE harbourview2026. Remaining dashboard item: the single WAF rate rule (path starts with /api, 30 requests per 60 seconds per IP, action 429), to be published and then verified with one scripted demo sequence confirming zero 429s. After that verification: deploy freeze.

## 2026-07-14 WAF verification and deploy freeze

The published rule (api-rate-limit, path starts with /api, fixed window 30 per 60 seconds per IP, 429) was verified two ways from one machine: the full scripted demo sequence ran clean with zero 429s (13.0 s), and an immediate 40-request burst drew 429s from request 27 onward, consistent with the smoke sharing the window. The rule both admits the demo and cuts off abuse.

DEPLOY FREEZE as of this entry. The frozen production build is gameloop-gilt.vercel.app. Remaining Thursday runbook: warmup from the venue network 15 minutes before, one full script run, Reset click, NHL endpoint curl to decide the live-game closer, access-code entry rehearsed. Backup capture is recorded by Saad from the frozen URL.

## 2026-07-15 Overnight styling session (branch style/overnight-pass, prod stays frozen)

Full visual overhaul on the branch per the overnight mission: dark-only arena-at-night design language (The Lit Sheet, see DESIGN.md), applied in passes with all three gates green at every commit. Production untouched; the freeze from 2026-07-14 stands.

Notable findings logged as they happened:

TraceEnvelope carries no timestamp, so the planned per-row elapsed clock in the decision log gutter would read 00:00.0 for every row in demo mode (frames arrive in one burst). The gutter stamps the scorer event number instead; total elapsed shows in the header only when it measures at least 100 ms, so live runs show real latency and rehearsals never show a dead clock.

An empirical contrast probe (canvas-resolved computed styles, all pages walked) plus a five-agent audit panel caught a cluster of AA failures in reduced-opacity frost text (ledger gutter, raw-event summaries, provenance strings, parentheticals, placeholders, char counter), all fixed by promoting to full-strength frost; the rule is now written into DESIGN.md. The probe initially missed these because Tailwind 4 emits color-mix() for opacity modifiers, which the first regex parser skipped silently.

Restyle-introduced regression caught and fixed: the new sticky header could cover programmatic focus targets (plan results, infeasible section, memory card); scroll-mt-20 added to all four. Also fixed: an unguarded hover transform on the home cards (reduced-motion guardrail), and the experimental relive form reusing the dashed border DESIGN.md reserves exclusively for SIMULATED provenance.

Pre-existing issues found by the keyboard audit, NOT fixed overnight (interaction behavior, not styling; for Saad to decide): (1) buttons that disable themselves on the interaction that activates them (plan submit, disruption buttons, relive buttons, enter submit) drop keyboard focus to body during streaming; predates the restyle. (2) SourceBadge supplementary context on the memory card is tooltip-only (title attribute), also pre-existing.

## 2026-07-15 Styled build deployed, freeze replaced

Saad approved lifting the freeze (merge and deploy instruction, 01:30). Protocol followed: style/overnight-pass fast-forwarded into master (6 commits, tip 35027df), conscious re-verify on master (155/155 vitest, clean build, local smoke 1/1), production deploy gameloop-ocv8skoss (dpl_54gtxThG6Uhc7yrBEKXDu3WeycYP) behind the frozen alias gameloop-gilt.vercel.app. Deployed smoke with the real access code passed in 14.6 s. Warmup re-triggered with an authenticated cookie: extraction grammar 1,852 ms, recap grammar 6,502 ms, consistent with the 2026-07-14 profile; grammar cache is roughly 24 h, so the Thursday venue-network warmup 15 minutes before the demo stays in the runbook.

DEPLOY FREEZE re-declared as of this entry on the styled build. Any further overnight work happens on a new branch with no pushes to master and no deploys without Saad.

## 2026-07-16 Overnight conversational session (branch feature/conversational-plan, prod stays frozen)

Mission: answerable clarifications, conversational refinement, assume-don't-interrogate, honest domain edges. All four goals shipped on the branch with gates green at every commit (final state: vitest 206/206, clean build, playwright 3/3 including the untouched scripted demo smoke). Prod untouched; the freeze stands.

Surprises logged as they happened:

The plan's pinned redirect copy said "Vegas Golden Knights at Carolina Hurricanes", which is backwards against the fixture (home is VGK) and quietly names a real host inside a fiction where Harbourview owns the venue. The Task 8 implementer's TDD run caught the contradiction between the plan's own code and its pinned test string and reported it instead of shipping either; the fix is neutral copy ("versus", home side first to mirror the code-built scoreLine order).

Two real product gaps surfaced by the new eval cases, both fixed deterministically with TDD rather than by weakening the eval expectations. First, an uncoverable hard dietary need (nut-free: no venue stand carries it) generated zero candidates, so the UI would have said "no feasible candidates generated" without naming the need and with no alternative; candidate generation now gates on coverable needs only, so the honest outcome is an infeasible result with the violation "dietary: no stand tonight offers nut-free", a bestAlternative, and a violated chip. Second, "actually we arrive at 6" extracts mode other (the model correctly refuses to invent train), which wiped the prior train mode on merge and made every candidate infeasible; the merge now preserves the prior travel mode on bare time changes, with a stated mode still replacing.

Temperature 0 on extraction (the baseline eval report's own recorded follow-up) converted two coin-flip cases into stable outcomes: abbreviated-asks now consistently asks the party clarification (the intended D1 behavior), while both paraphrase cases now consistently drop the implicit food_preference in "warmups matter more than food variety" phrasing. Baseline block holds at 11 of 13, the same count as the recorded baseline, same residual class; the candidate one-line prompt fix is documented in evals/report-conversational.md and deliberately not applied the night before the demo.

The Claude Code process restarted mid fix-wave; the interrupted subagent was resumed from its saved transcript and completed with no work lost. Worth knowing: session-only crons survive a context reset but not a process exit, so the heartbeat should be checked after any restart.

Live end-to-end check (real key, local production build): the verbatim basketball-halal prompt redirects honestly and still plans (halal via Anchor Smokehouse with the cross-contact disclaimer), and a free-text "actually we arrive at 6" follow-up merges as an added arrival. Because that new arrival has no prior mode, the planner falls back to a doors-open walk-in and honestly marks the arrival chip traded rather than dead-ending; offering the nearest train explicitly for mode-less new arrivals is a recorded polish candidate, not a defect.

## 2026-07-16 Conversational branch merged to master, deploy still pending

On Saad's instruction after his morning review: feature/conversational-plan fast-forwarded into master (21 commits, tip 9dcc1e7), conscious re-verify on master green (206/206 vitest, clean build, playwright 3/3 including the untouched scripted demo smoke). Local branches feature/conversational-plan and style/overnight-pass deleted; nothing pushed to origin. Production remains the frozen styled build at gameloop-gilt.vercel.app; the DEPLOY FREEZE from 2026-07-15 still stands until Saad runs the remaining protocol steps (deploy, deployed smoke with the real access code, one rehearsal of the two new demo beats, venue warmup 15 minutes before).

## 2026-07-16 Conversational build deployed, freeze replaced

Saad approved push and deploy. master pushed to origin (0f56de8..6941c88, plus a BUILDLOG record commit); the GitHub default branch was found still pointing at style/overnight-pass, which blocked deleting that stale remote branch, so the default was corrected to master and the branch removed. Production deploy gameloop-i45x6cety behind the frozen alias gameloop-gilt.vercel.app, Ready in 45 seconds. Deployed smoke with the real access code: 3 of 3 in 18.5 seconds, covering the original scripted sequence (17.3 s, within its historical range) plus both conversational flows, which doubles as proof the alias serves the new build. Warmup re-triggered with an authenticated cookie: extraction grammar 8,920 ms (a fresh compile, expected because PlanRequestSchema gained eventMismatch), recap grammar 6,677 ms (consistent with prior profiles). Grammar cache is roughly 24 hours, so the venue-network warmup 15 minutes before the demo stays in the runbook.

DEPLOY FREEZE re-declared as of this entry on the conversational build. Remaining demo-day runbook: one rehearsal of the two new beats (Short on details answered inline; Arriving at 6:00 instead), the venue warmup, the Reset click, and the NHL endpoint curl for the live-game closer.

## 2026-07-19 to 07-20 Polish pass (branch style/polish-pass, prod stays frozen)

Saad used the deployed app after the demo cycle and was not wowed: the verdict was that it felt incomplete. A four-lens research workflow (visual critique over 22 production screenshots, code completeness audit, live UX walk at desktop and 390px, and state-of-the-art research into 2026 AI-product UI) converged on one diagnosis: the app was styled but not composed. The single elevated surface went to a 17-row developer decision log rendering first at about 1973px tall while Tonight's Plan rendered below it flat, and internal strings (pipe-delimited candidate keys, raw float scores, plan ids, the misleading narrative-unavailable caveat) reached primary copy. Full plan in docs/superpowers/2026-07-19-polish-research.md, twelve ranked items, all zero new dependencies.

Saad approved all three waves, including the conscious reversal of the DESIGN.md thesis that the Decision Log alone gets the Ice treatment. Eleven commits, gates green at every one, tip 399d0bc, vitest grew 206 to 243.

Wave 1 (a2f7d7d, 4970cf3, f9e2c5c, 948ff6b, bbeaae4): the hero flip (plan first in the ice sheet, log collapsed to a Plan built from N signals strip that opens while streaming), the itinerary as a true vertical timeline with glyphs and accent dots, every leaked internal stripped from prose including decisionSummary itself (tests-first in the deterministic core), dot-plus-word severity chips, the display-face hero sentence, 44px mobile targets, honest error states. The opus wave review caught a genuine blocker the green gates missed: React fires toggle events for programmatic open changes, so the log auto-collapse wedged open on any replan that started collapsed. Reproduced empirically, fixed by making the summary click preventDefault so React alone owns the open state, and the collapse contract is now pinned by the smoke.

Wave 2 (a04391c, ee3cb0c, b5226f5): the tonight's-game eyebrow with SNAPSHOT provenance and a countdown that only renders inside a sane 12-hour window (normalized minutes, never string comparison; the midnight cases all degrade honestly to the static form), the ready-state CTA outline chosen so it cannot suppress the keyboard focus ring, the memory panel empty-state preview, and the skeleton timeline that fills the hero while a plan builds. The wave review caught the plan page shipping both 26KB showcase fixtures to the client to render four scalars; fixed with a server component split plus a loader module split, verified by grepping the built chunks (api-web.nhle.com occurrences went two to zero).

Wave 3 (f623ee5, 399d0bc): the lamp field raised to perceptible (0.09 and 0.065, AA verified by sampling rendered pixels, frost at 6.84 to 1), one accent per context (sodium pre-plan eyebrow rule and streaming row bar, ice-green on the finished plan label), first-paint arrivals via starting-style nested in the single reduced-motion block, relive card accents grounded in each card's own copy, and a plain-language provenance lead on how-it-works that renders the real badges.

Final whole-branch review (opus): READY, no blockers, no important findings. The reviewer independently re-verified the collapse cycle on the exact previously-wedged path, recomputed the AA math, walked the full UX in a real browser at both widths, and confirmed the client chunks carry no fixture or secret. Nine residual minors inventoried in the review for post-merge cleanup, including two pre-existing recap wording items this branch did not touch.

Nothing pushed, nothing deployed. Production remains gameloop-i45x6cety behind gameloop-gilt.vercel.app; the DEPLOY FREEZE from 2026-07-16 still stands until Saad runs the protocol (merge, conscious re-verify on master, deploy, deployed smoke with the real access code, warmup).

## 2026-07-20 Polish pass merged and deployed, freeze replaced

Saad approved merge and deploy. style/polish-pass fast-forwarded into master (12 commits, tip 2dfd64b), conscious re-verify on master green (vitest 243/243, clean build, playwright 3/3), pushed to origin (da6ebb5..2dfd64b), branch deleted. Production deploy gameloop-l0vn7tgb3 behind the frozen alias gameloop-gilt.vercel.app, Ready in 36 seconds. Deployed smoke with the real access code: 3 of 3 in 16.1 seconds, and because the smoke now asserts the flipped hero selectors and the log collapse contract, the pass doubles as proof the alias serves the polished build; the scripted sequence ran 15.2 seconds, inside its historical range. Warmup re-triggered with an authenticated cookie: extraction 1,925 ms and recap 5,115 ms, a warm-cache profile as expected since this branch changed no schemas.

DEPLOY FREEZE re-declared as of this entry on the polished build. The post-merge cleanup list lives in the 2026-07-19 entry and the final review: shared plan-label helper, hero-sentence badge ratification, stale infeasible card during replan, the recap section-101 wording, and the how-it-works LIVE-badge overclaim.

## 2026-07-20 to 07-21 Chat workspace build (branch feature/chat-workspace, prod stays frozen)

Mission: turn Plan My Night from a stack of panels into a conversation, per the approved spec docs/superpowers/specs/2026-07-20-chat-workspace-design.md. The design is a two-region workspace, the message thread on the left and the polished plan hero relocated into a persistent panel on the right, composed entirely from the existing SSE TraceEnvelope stream with no API, schema, or lib/ai change. Two deterministic additions only: the July 25 weekend service disruption and a reduced real-places research fixture with its own schema, loader, and dietary filter. Full plan in docs/superpowers/plans/2026-07-20-chat-workspace.md, twelve tasks in three waves, zero new dependencies.

The plan was written on the main thread from the spec and the July 25 research (research/2026-07-25-real-data/), then executed with subagent-driven development per CLAUDE.md: sonnet implemented each task from a self-contained brief, opus reviewed per wave, models passed explicitly. Gates green at every commit (final state: vitest 280 up from 232, clean build, clean tsc, playwright 4 up from 3, the poisoned-key webServer byte-identical). Prod untouched; the freeze from 2026-07-20 stands.

Wave 1 (eb59deb, 2ebf357, ccb48d1, 6962ea3, 4d7b711): the july25-weekend-service disruption built tests-first in the deterministic core (it thins Lakeshore West to its hourly West Harbour base trips, modeling the verified Ontario Line construction weekend, which re-snaps an 18:18 train arrival from the 18:15 Lakeshore West to the 18:12 Lakeshore East and stacks with train-plus-18 to 18:30); the real-nearby Zod schema with three evidence tiers (certified, self-described, friendly) and a deterministic dietary filter whose nut-free and dairy-free paths render an honest absence rather than options; the nine-entry fixture authored from the research candidates, only weekend-evening-open restaurants, no invented ratings, no nut-free or dairy-free evidence anywhere; and the chat and real-places copy. The opus wave review approved and caught one real leak the green gates missed: the union-chicken review note said "Toronto-area listings", and since the card renders that field the real city would have reached the UI in contradiction of the design's own never-name-the-city guard. Fixed by dropping the city from two notes, and a fixture test now asserts no renderable text field names the city or the real arena.

Wave 2 (d48e65e, 3da8ed1, 0765ba4, 02558a5, e1387fb, dd67e87): the pure composeAssistantTurn (stream envelopes to turn segments: redirect, adjustments, assumptions, clarification, body, plan result, error, log rows), verified by fixture against every emitter path; the ReasoningDisclosure, the polish pass decision log relocated into the turn with its collapse contract intact and its toggle-wedge regression tests moved with it; the ChatComposer absorbing the suggested prompts and quick chips with the demo free-text disable preserved, and PartyAnswerForm extracted; the NearbyRealOptions card; the July 25 quick action; and the message thread components. The opus wave review approved and flagged one should-fix: an infeasible turn rendered the "cannot be satisfied" line twice, once from the narrative fallback whose text still pointed at a "below the Decision Log" region that no longer exists; folded the one-line suppression plus two nits (index-stable assumption keys, a fresh-composer demo note naming the suggested prompts instead of the not-yet-visible quick chips) into the Wave 3 commit.

Wave 3 (e929206): the single large commit the spec required, so gates never go red between commits. PlanClient split into a PlanWorkspace orchestrator plus PlanPanel, with the state machine carried over intact (the subtle load-bearing detail: a fresh, non-refined disruption replan re-sends the chip id so demo mode re-derives the same contract); the conversation ledger replacing the removed history section; both Playwright specs rewritten to the chat selectors keeping the same flows plus a new 390px mobile pass, with the demo sequence running July 25 before train-plus-18 so the 18:15 to 18:12 re-snap is visible before the 18:30 delay; ActivityPanel, ConstraintContract, FollowUpComposer, and PlanClient deleted; one .turn-arrive rule added inside the single reduced-motion block. The final opus review returned APPROVE with no blockers and three nits: deleted the three orphaned copy keys the swap left behind, and locked the composer and disruption controls through a stalled stream as well as a streaming one so a stall can never freeze a turn as done; the remaining one-frame live-mode flash is left for hand review.

Nothing pushed, nothing deployed. Production remains gameloop-l0vn7tgb3 behind gameloop-gilt.vercel.app; the DEPLOY FREEZE stands until Saad runs the protocol (merge, conscious re-verify on master, deploy, deployed smoke with the real access code, warmup). The July 25 real-data research also surfaced two items outside this build's scope, banked for Saad: the NHL.com 2026-27 opener coverage calls Carolina the defending champion, which conflicts with the showcase fixture where Vegas won, and the real July 25 anchor event is a Tame Impala concert, not a hockey game, so tonight stays the replayed Stanley Cup Final game.

## 2026-07-21 Chat workspace merged and deployed, freeze replaced

Saad's instruction: push, merge, deploy, and run a deployed e2e. feature/chat-workspace fast-forwarded into master (16 commits, tip 3398735), conscious re-verify on master green (vitest 280, clean tsc, clean build, playwright 4 including the 390px mobile spec), pushed to origin (7d4c1c3..3398735). Production deploy via vercel --prod, remote build, deployment gameloop-81cc2sgwq (dpl_BEAA7tbFu9WyGM9BCkQsm2EGob4R), Ready in 33s; the prod deploy auto-reassigned the demo alias gameloop-gilt.vercel.app to the new deployment. Previous build gameloop-l0vn7tgb3 stays Ready for instant rollback.

Deployed verification, everything reachable without the private access code: the alias serves the new build (homepage shows Plan My Night with no Relive references, /relive returns 404, how-it-works carries the new provenance lead), the /enter shell renders, a wrong code returns the honest "not recognized" error through a real deployed API round-trip and stays on /enter, and the real gate holds (POST /api/plan without a cookie returns 401). The authenticated happy-path browser flow (enter code, plan, July 25 and train disruptions, real-places card, reset) was NOT run against production because the prod ACCESS_CODE is an encrypted secret that vercel env pull withholds and the local .env.local code does not match prod; that flow is covered at high confidence by the full 4/4 Playwright suite passing locally against a production build of this exact commit, but the against-prod authenticated run is left for Saad, who has the code. The demo-day warmup (re-trigger the extraction and recap grammars 15 minutes before, grammar cache is roughly 24 hours) also needs the authenticated cookie and stays in the runbook.

Remaining demo-day runbook for Saad: run the one authenticated deployed smoke with the real code (SMOKE_ACCESS_CODE=<code> PLAYWRIGHT_BASE_URL=https://gameloop-gilt.vercel.app npx playwright test), the venue warmup 15 minutes before, and a rehearsal of the new beats (suggested prompt to plan, July 25 weekend service, the real-places card). DEPLOY FREEZE re-declared on the chat workspace build as of this entry.

## 2026-07-21 Access code reset, authenticated deployed smoke green

Follow-up to the deploy above. The prod ACCESS_CODE turned out to be a Sensitive (write-only) Vercel var that could not be recovered, so on Saad's instruction it was reset (vercel env rm/add ACCESS_CODE production) to "letmein" to match the local .env.local, then a fresh prod deploy (gameloop-4hvevkcro) picked it up and re-aliased gameloop-gilt.vercel.app. Gate re-verified (letmein 200, wrong code 401). The full authenticated Playwright suite then ran green against production, 4 of 4 in 5.6s: the scripted demo sequence (access, suggested prompt, July 25 weekend service, train delay, real-places card, reset) plus the two conversational flows and the 390px mobile pass. This closes the one gap noted above; the deployed chat workspace is now end-to-end verified against prod. Saad stated the project will be shut down after today's interview and that the code may leak, so "letmein" is recorded rather than treated as a durable secret. The demo-day grammar warmup (needs the now-known code) is the only remaining runbook item.

## 2026-07-21 Demo-narrative SSE streaming shipped

Saad flagged that back-and-forth replies "just pop up" and asked for SSE streaming. Diagnosis: the app already streams over SSE (the reasoning log frame by frame, and in live mode the model narrative token by token); what popped was the demo-mode deterministic reply, emitted as one response_chunk. Fix: a pure chunkNarrative helper slices the narrative into contiguous word groups (concatenation byte-identical to the original, unit-tested seven ways) which app/api/plan/route.ts emits as separate response_chunk frames about 45ms apart over the existing transport, abort-aware; lib/ai and the live explainPlanStream path untouched. A sonnet subagent implemented it from a scoped brief; the main-thread review caught and fixed an abort-listener that was not removed on normal completion (would trip Node's ten-listener warning on a long narrative). Gates green (vitest 280 to 287, clean tsc, clean build, playwright 4). Branch feature/stream-demo-narrative fast-forwarded into master (tip 06b00ab), pushed origin, deployed vercel --prod (gameloop-attw1sa8r), demo alias reassigned. Verified on the live alias: gate 200, one demo plan now delivers 14 response_chunk frames over about 1.2s (was a single instant frame), and the authenticated Playwright suite passed 4 of 4 against prod. Rollback target gameloop-4hvevkcro stays Ready. A parallel sonnet agent had re-confirmed the pre-streaming prod build 4 of 4 first. Preview-URL delivery was attempted but abandoned: the Vercel CLI would not persist Preview-scoped env vars non-interactively, and shipping straight to prod on Saad's go was cleaner.

## 2026-07-21 Decision log folded during streaming, completion invite, slimmer footer

Saad's note on the Plan My Night flow: when a message is sent the decision log expands over the streaming reply, so the SSE narrative lands off screen; he asked to keep the reasoning log folded and to have it flash after the reply so he can open it on his own. Plus the footer read too thick. Fix, all in the client, no API, schema, or lib/ai change: ReasoningDisclosure now stays folded through the whole turn (the streaming narrative below it is never displaced), and on the live streaming-to-done transition it raises an invite flag rather than opening itself. The invite is a durable state the reader can see, not just motion: an unread sodium dot on the summary, the summary text brightened to ice, and a short finite invite-glow animation (three iterations) added only inside the existing reduced-motion block, so reduced-motion users still get the dot and the brighter text with no pulse. A manual open answers the invite (clears the class and the dot) and wins for the rest of the turn; the next fresh stream resets everything. The collapse contract already covered by tests kept its shape, so the manual-toggle-wins and mounts-folded cases carried over; the contract test was rewritten to assert folded-while-streaming plus the new invite flag, and the stale "opens while streaming" comment in the demo smoke helper was corrected (the guarded expandReasoning helper still works, it now always has to click to open). Footer padding slimmed py-5 to py-3. Gates green (vitest 287, clean tsc, clean build, playwright 4, eslint at parity: the same 20 pre-existing problems in the untouched ledger-clock ref block, zero introduced). Branch polish/reasoning-fold-footer fast-forwarded into master (tip 754c384), pushed origin, deployed vercel --prod (gameloop-ge6qwqhk9, dpl_1bwP1U5LJRZVbCpkGes6KojEA4Lx), demo alias reassigned to it. A sonnet subagent verified prod end to end: the alias serves the new build (footer py-3 in the served HTML), the unauthenticated gate holds (/enter 200, POST /api/plan without a cookie 401), the authenticated Playwright suite passed 4 of 4 against prod, and a throwaway targeted spec confirmed the three new behaviors on the live site (the log stayed folded across seven samples in the first 800ms of a demo stream, on completion it carried the log-invite class and the unread dot and cleared both when clicked open, the narrative text rendered in the turn, and the footer computed padding-top was 12px). Rollback target gameloop-attw1sa8r stays Ready. DEPLOY FREEZE re-declared on this build as of this entry.

## 2026-07-21 Footer slimmed further after review

Saad reported the footer still read too thick after the py-3 pass above. Measured the rendered block with a throwaway Playwright script (headless chromium, 1440px, computed styles plus boundingBox): the deployed footer was 97px, and the bulk was not the padding but the loose leading-5, which forces 20px line boxes on 12px text across three disclaimer lines. Fix: line-height to leading-tight (15px per line), padding py-3 to py-2, inter-line gap gap-1.5 to gap-1; the three disclaimers are unchanged (provenance honesty keeps all three). Re-measured 70px, a 28 percent reduction from the live build and about 38 percent from the original py-5, verified against a before/after screenshot pair. Gates green (clean tsc, clean build, playwright 4). Branch polish/footer-slimmer fast-forwarded into master (tip 88c483b), pushed origin, deployed vercel --prod (gameloop-9mcuohyuc), demo alias reassigned; the served homepage HTML on the alias now carries px-6 py-2 leading-tight on the footer. Rollback target gameloop-ge6qwqhk9 stays Ready. DEPLOY FREEZE re-declared on this build as of this entry.
